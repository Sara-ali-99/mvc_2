@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
<form action="/Department/addToProject">
    <label>choose employee: </label>
    <select name="emp" id="emp">
        @foreach(var emp in Model)
        {
            <option value="@emp.SSN">@emp.FirstName @emp.MiddleName @emp.LastName</option>
        }    
    </select>
    <label >choose project: </label>
    <select name="projs" id="projs" multiple >
        @foreach (var p in ViewBag.dps)
        {
            <option  value="@p.Number" >@p.Name</option>
        }
    </select>
    <input type="submit" value="Add"/>
</form>